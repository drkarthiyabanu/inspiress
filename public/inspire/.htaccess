Options +FollowSymLinks
RewriteEngine On
RewriteBase /

# Skip rewrite if it's a real file or directory
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Rewrite clean URLs like /courses to /courses.php
RewriteCond %{REQUEST_FILENAME}.php -f
RewriteRule ^(.+)$ $1.php [L]
